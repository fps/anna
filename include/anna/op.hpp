#pragma once

namespace anna
{
  template<typename T, int InputChannels, int OutputChannels, int MaxBlockSize>
  struct linear
  {
    template<typename NextOpType>
    inline void process(const int n)
    {

    } 
  };
}
